import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'
import { withInstall } from '@dawn-ui/utils'

export const DButton = withInstall(Button)
export const DButtonGroup = withInstall(ButtonGroup)